# Prettier also reads .gitignore by default, so node_modules, dist, coverage
# and .env* need no entry here. Only tracked paths belong below.

# Demos have their own toolchain (demo/svelte) or are not JS (demo/esp32*)
demo

package-lock.json
